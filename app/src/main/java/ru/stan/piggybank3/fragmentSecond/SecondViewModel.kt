package ru.stan.piggybank3.fragmentSecond

import androidx.lifecycle.ViewModel
import ru.stan.piggybank3.data.Moneyboxe

class SecondViewModel : ViewModel() {
    fun addMoneyboxe(moneyboxe: Moneyboxe) {
        // Добавьте код для добавления нового элемента в список данных (например, в вашем LiveData)
    }

}